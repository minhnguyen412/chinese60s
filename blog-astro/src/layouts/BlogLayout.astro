---
// src/layouts/BlogLayout.astro
// Dùng với Astro Content Collections + remark-directive
// npm install remark-directive

export interface TocItem {
  id: string;
  label: string;
}

export interface NavLink {
  href: string;
  title: string;
}

export interface Props {
  title: string;
  description?: string;
  tag?: string;
  date?: string;
  readTime?: string;
  author?: string;
  heroChar?: string;
  toc?: TocItem[];
  prevPost?: NavLink;
  nextPost?: NavLink;
}

const {
  title,
  description = "",
  tag = "📚 Learning Guide",
  date = "2025",
  readTime = "5 min read",
  author = "Chinese60s Team",
  heroChar = "学",
  toc = [],
  prevPost,
  nextPost,
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <meta name="author" content="Chinese60s Team" />
  <title>{title} | Chinese60s</title>
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3235921032748480" crossorigin="anonymous"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&display=swap" rel="stylesheet" />
</head>
<body>

  <div id="progress-bar"></div>

  <!-- ── HEADER ── -->
  <header>
    <a href="/index.html" class="brand"><h1>CHINESE 60s</h1></a>
    <button class="menu-toggle" aria-label="Toggle navigation">&#9776;</button>
    <nav>
      <ul>
        <li><a href="/about.html">About Us</a></li>
        <li><a href="/blog.html">Blog</a></li>
        <li><a href="/courses.html">Courses</a></li>
        <li><a href="https://www.amazon.com/shop/studiorum60s" target="_blank">Shop</a></li>
      </ul>
    </nav>
  </header>

  <!-- ── HERO ── -->
  <div class="article-hero" style={`--hero-char: "${heroChar}"`}>
    <div class="breadcrumb">
      <a href="/index.html">Home</a><span>›</span>
      <a href="/blog.html">Blog</a><span>›</span>
      <span>Article</span>
    </div>
    <span class="article-tag">{tag}</span>
    <h1>{title}</h1>
    <div class="article-meta">
      <span class="meta-item">📅 {date}</span>
      <span class="meta-sep">·</span>
      <span class="meta-item">⏱ {readTime}</span>
      <span class="meta-sep">·</span>
      <span class="meta-item">✍️ {author}</span>
    </div>
  </div>

  <!-- ── PAGE LAYOUT ── -->
  <div class="page-layout">

    <main>
      <article class="article-body" id="article-top">

        <!-- Nội dung markdown được render vào đây -->
        <slot />

        <!-- Post navigation -->
        {(prevPost?.href || nextPost?.href) && (
          <div class="post-nav">
            <div>
              {prevPost?.href && (
                <a href={prevPost.href}>
                  <span class="nav-dir">← Previous</span>
                  <span class="nav-title">{prevPost.title}</span>
                </a>
              )}
            </div>
            <div>
              {nextPost?.href && (
                <a href={nextPost.href} class="next">
                  <span class="nav-dir">Next →</span>
                  <span class="nav-title">{nextPost.title}</span>
                </a>
              )}
            </div>
          </div>
        )}
      </article>

      <!-- Comments -->
      <div class="comments-section">
        <h3>💬 Comments</h3>
        <div id="disqus_thread"></div>
        <noscript>Please enable JavaScript to view the <a href="https://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>
      </div>
    </main>

    <!-- ── SIDEBAR ── -->
    <aside class="sidebar">

      {toc.length > 0 && (
        <div class="sidebar-card">
          <h3>📋 In this article</h3>
          <ul class="toc-list">
            {toc.map((item, i) => (
              <li>
                <a href={`#${item.id}`}>
                  <span class="toc-num">{i + 1}</span>
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      )}

      <div class="sidebar-card">
        <h3>📝 Latest Posts</h3>
        <ul class="latest-list">
          <li><a href="/blog/blog-2.html"><span class="lp-num">2</span>What Should You Do When Starting to Learn Chinese?</a></li>
          <li><a href="/blog/blog-3.html"><span class="lp-num">3</span>The True Nature of Learning Chinese</a></li>
          <li><a href="/blog/blog-4.html"><span class="lp-num">4</span>How to Keep Learning When You're Busy</a></li>
          <li><a href="/blog/blog-5.html"><span class="lp-num">5</span>How to Effectively Learn New Vocabulary</a></li>
        </ul>
      </div>

      <div class="sidebar-donate">
        <span class="don-emoji">☕</span>
        <h3>Support Chinese60s</h3>
        <p>If our content helps you learn, consider buying us a coffee!</p>
        <a href="https://buymeacoffee.com/chinese60s" target="_blank">☕ Donate</a>
      </div>

    </aside>
  </div>

  <div class="donate-tag">
    <a href="https://buymeacoffee.com/chinese60s" target="_blank">☕ Donate</a>
  </div>

  <footer>
    <p class="brand">CHINESE 60s</p>
    <p>Learn Mandarin through images and videos</p>
    <div class="social-icons">
      <a href="#">Facebook</a>
      <a href="#">YouTube</a>
      <a href="#">TikTok</a>
      <a href="#">Instagram</a>
    </div>
  </footer>

</body>
</html>

<script>
  const toggle = document.querySelector('.menu-toggle') as HTMLButtonElement;
  const navEl  = document.querySelector('nav') as HTMLElement;
  toggle.addEventListener('click', () => {
    navEl.classList.toggle('show');
    toggle.innerHTML = navEl.classList.contains('show') ? '✕' : '☰';
  });

  const bar = document.getElementById('progress-bar') as HTMLElement;
  window.addEventListener('scroll', () => {
    const scrollTop = window.scrollY;
    const docHeight = document.documentElement.scrollHeight - window.innerHeight;
    bar.style.width = Math.min(100, (scrollTop / docHeight) * 100) + '%';
  });

  const tocLinks = document.querySelectorAll<HTMLAnchorElement>('.toc-list a');
  const headings = Array.from(document.querySelectorAll<HTMLElement>('h2[id], .step-card[id]'));
  const tocObserver = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        tocLinks.forEach(a => a.classList.remove('active'));
        const active = document.querySelector<HTMLAnchorElement>(`.toc-list a[href="#${entry.target.id}"]`);
        if (active) active.classList.add('active');
      }
    });
  }, { rootMargin: '-20% 0px -70% 0px' });
  headings.forEach(h => tocObserver.observe(h));

  (function () {
    const d = document, s = d.createElement('script');
    s.src = 'https://chinese60s-com.disqus.com/embed.js';
    s.setAttribute('data-timestamp', String(+new Date()));
    (d.head || d.body).appendChild(s);
  })();
</script>

<style is:global>
  :root {
    --black:         #222;
    --green:         #7cb83e;
    --green-dark:    #5a8f27;
    --green-light:   #d8f0a0;
    --green-pale:    #f2fde0;
    --yellow:        #ffd84d;
    --yellow-pale:   #fffbe6;
    --sky-pale:      #eaf8ff;
    --white:         #fff;
    --bg:            #fffef5;
    --shadow-sm:     3px 3px 0 var(--black);
    --shadow-md:     4px 4px 0 var(--black);
    --shadow-lg:     6px 6px 0 var(--black);
    --border:        2.5px solid var(--black);
    --border-thick:  3px solid var(--black);
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    font-family: 'Fredoka', sans-serif; font-weight: 500;
    background: var(--bg); color: var(--black);
    overflow-x: hidden; display: flex; flex-direction: column; min-height: 100vh;
  }
  body::before {
    content: ''; position: fixed; inset: 0;
    background-image: radial-gradient(circle, rgba(0,0,0,0.04) 1px, transparent 1px);
    background-size: 22px 22px; pointer-events: none; z-index: 0;
  }

  /* ── PROGRESS BAR ── */
  #progress-bar {
    position: fixed; top: 0; left: 0; height: 4px;
    background: var(--green); width: 0%; z-index: 200;
    border-right: 2px solid var(--green-dark); transition: width 0.1s linear;
  }

  /* ── HEADER ── */
  header {
    background: var(--green); border-bottom: var(--border-thick);
    padding: 0 50px; height: 66px;
    display: flex; justify-content: space-between; align-items: center;
    position: sticky; top: 0; z-index: 100;
  }
  header a.brand { text-decoration: none; }
  header h1 { font-weight: 700; font-size: 1.7rem; color: white; letter-spacing: 0.04em; text-shadow: 2px 2px 0 rgba(0,0,0,0.25); }
  .menu-toggle { display: none; font-size: 24px; background: none; border: none; color: white; cursor: pointer; }
  nav ul { list-style: none; display: flex; gap: 8px; }
  nav ul li a {
    text-decoration: none; color: var(--black); font-weight: 600; font-size: 1rem;
    background: var(--white); border: var(--border); border-radius: 50px;
    padding: 6px 18px; display: inline-block;
    box-shadow: var(--shadow-sm); transition: transform 0.12s, box-shadow 0.12s;
  }
  nav ul li a:hover { transform: translate(-2px,-2px); box-shadow: 5px 5px 0 var(--black); }

  @media (max-width: 768px) {
    header { padding: 0 20px; position: relative; }
    .menu-toggle { display: block; }
    nav { display: none; position: absolute; top: 66px; left: 0; right: 0; background: var(--green); border-bottom: var(--border); padding: 12px 0; }
    nav.show { display: flex; flex-direction: column; align-items: center; }
    nav ul { flex-direction: column; align-items: center; gap: 8px; padding: 6px 0; }
  }

  /* ── HERO ── */
  .article-hero {
    background: var(--green-pale); border-bottom: var(--border-thick);
    padding: 52px 24px 44px; text-align: center;
    position: relative; overflow: hidden; z-index: 1;
  }
  .article-hero::before {
    content: var(--hero-char, "学");
    position: absolute; font-size: 260px; font-family: serif;
    color: rgba(0,0,0,0.05); top: -30px; right: 20px;
    line-height: 1; pointer-events: none;
  }
  .article-hero .breadcrumb {
    display: flex; align-items: center; justify-content: center; gap: 8px;
    margin-bottom: 18px; font-size: 0.88rem; font-weight: 600; color: #666;
  }
  .article-hero .breadcrumb a { color: var(--green-dark); text-decoration: none; font-weight: 700; }
  .article-hero .breadcrumb a:hover { text-decoration: underline; }
  .article-hero .breadcrumb span { color: #bbb; }
  .article-tag {
    display: inline-block; background: var(--green-light); border: var(--border);
    border-radius: 50px; padding: 5px 16px; font-weight: 700; font-size: 0.88rem;
    box-shadow: var(--shadow-sm); margin-bottom: 16px; color: var(--green-dark);
  }
  .article-hero h1 {
    font-weight: 700; font-size: clamp(1.7rem, 4vw, 2.8rem);
    line-height: 1.25; max-width: 780px; margin: 0 auto 18px;
  }
  .article-meta {
    display: flex; align-items: center; justify-content: center;
    gap: 16px; flex-wrap: wrap; font-size: 0.88rem; font-weight: 600; color: #777;
  }
  .article-meta .meta-item { display: flex; align-items: center; gap: 5px; }
  .article-meta .meta-sep { color: #ccc; }

  /* ── PAGE LAYOUT ── */
  .page-layout {
    flex: 1; max-width: 1180px; margin: 0 auto;
    padding: 52px 24px 72px;
    display: grid; grid-template-columns: 1fr 300px; gap: 48px;
    align-items: start; position: relative; z-index: 1; width: 100%;
  }
  @media (max-width: 1024px) { .page-layout { grid-template-columns: 1fr; gap: 40px; } .sidebar { order: -1; } }
  @media (max-width: 768px) { .page-layout { padding: 36px 16px 60px; } }

  /* ── ARTICLE BODY ── */
  .article-body {
    background: var(--white); border: var(--border-thick); border-radius: 24px;
    box-shadow: var(--shadow-lg); padding: 44px 48px; min-width: 0;
  }
  @media (max-width: 768px) { .article-body { padding: 28px 22px; } }

  /* ── TYPOGRAPHY (áp dụng cho cả markdown render) ── */
  .article-body h2 {
    font-weight: 700; font-size: 1.55rem; line-height: 1.3;
    margin: 36px 0 14px; padding-left: 16px;
    border-left: 5px solid var(--green); color: var(--black);
  }
  .article-body h3 {
    font-weight: 700; font-size: 1.2rem; line-height: 1.3;
    margin: 28px 0 10px; display: flex; align-items: center; gap: 10px;
  }
  .article-body h3::before {
    content: ''; display: inline-block; width: 10px; height: 10px;
    background: var(--yellow); border: 2px solid var(--black);
    border-radius: 50%; flex-shrink: 0; box-shadow: 1px 1px 0 var(--black);
  }
  .article-body p { font-size: 1.05rem; line-height: 1.8; color: #444; margin-bottom: 16px; }
  .article-body p:last-child { margin-bottom: 0; }
  .article-body strong { font-weight: 700; color: var(--black); }
  .article-body em { font-style: italic; color: #666; }
  .article-body hr { border: none; border-top: var(--border); margin: 36px 0; }

  /* ── .lead (custom directive :::lead) ── */
  .article-body .lead {
    font-size: 1.15rem; line-height: 1.85; color: #555; margin-bottom: 28px;
    padding: 20px 24px; background: var(--sky-pale);
    border: var(--border); border-radius: 18px; box-shadow: var(--shadow-sm);
  }
  .article-body .lead p { font-size: inherit; color: inherit; margin: 0; }

  /* ── LISTS ── */
  .article-body ul {
    list-style: none; margin: 12px 0 20px;
    display: flex; flex-direction: column; gap: 10px;
  }
  .article-body ul li {
    display: flex; gap: 12px; align-items: flex-start;
    font-size: 1.02rem; line-height: 1.65; color: #444; font-weight: 500;
  }
  .article-body ul li::before {
    content: '→'; color: var(--green-dark); font-weight: 700;
    flex-shrink: 0; margin-top: 1px;
  }

  /* ── LINKS ── */
  .article-body a { color: var(--green-dark); font-weight: 700; text-decoration: underline; text-underline-offset: 3px; }
  .article-body a:hover { color: var(--green); }

  /* ── IMAGES — markdown img → .post-img style ── */
  .article-body img {
    display: block; width: 100%; max-width: 620px; height: auto;
    margin: 20px auto; border: var(--border-thick); border-radius: 18px;
    box-shadow: var(--shadow-md);
  }

  /* ── .tip-box (custom directive :::tip) ── */
  .tip-box {
    background: var(--yellow-pale); border: var(--border-thick); border-radius: 18px;
    padding: 18px 22px; margin: 24px 0; box-shadow: var(--shadow-sm);
    display: flex; gap: 14px; align-items: flex-start;
  }
  .tip-box::before {
    content:  font-size: 1.5rem; flex-shrink: 0;
    /* fallback nếu không có data-icon */
  }
  .tip-box p { margin: 0; font-size: 1rem; color: #555; line-height: 1.7; }

  /* ── .step-card (custom directive :::step) ── */
  .step-card {
    background: var(--white); border: var(--border-thick); border-radius: 20px;
    box-shadow: var(--shadow-sm); padding: 20px 22px; margin-bottom: 16px;
    display: flex; gap: 18px; align-items: flex-start;
    transition: transform 0.12s, box-shadow 0.12s;
  }
  .step-card:hover { transform: translate(-2px,-2px); box-shadow: var(--shadow-md); }
  .step-num {
    width: 38px; height: 38px; border-radius: 50%;
    background: var(--green); color: white; border: var(--border);
    box-shadow: var(--shadow-sm); display: flex; align-items: center; justify-content: center;
    font-weight: 700; font-size: 1rem; flex-shrink: 0;
  }
  .step-body h4 { font-weight: 700; font-size: 1.1rem; margin-bottom: 5px; }
  .step-body p { font-size: 0.97rem; color: #555; line-height: 1.65; margin: 0; }

  /* ── POST NAV ── */
  .post-nav { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-top: 44px; }
  .post-nav a {
    background: var(--white); border: var(--border-thick); border-radius: 18px;
    box-shadow: var(--shadow-sm); padding: 16px 20px; text-decoration: none;
    color: var(--black); display: flex; flex-direction: column; gap: 4px;
    transition: transform 0.12s, box-shadow 0.12s;
  }
  .post-nav a:hover { transform: translate(-2px,-2px); box-shadow: var(--shadow-md); }
  .post-nav a .nav-dir { font-size: 0.8rem; font-weight: 700; color: #aaa; text-transform: uppercase; letter-spacing: 0.07em; }
  .post-nav a .nav-title { font-size: 0.95rem; font-weight: 700; line-height: 1.4; color: var(--green-dark); }
  .post-nav a.next { text-align: right; }
  @media (max-width: 560px) { .post-nav { grid-template-columns: 1fr; } }

  /* ── COMMENTS ── */
  .comments-section {
    margin-top: 44px; background: var(--white); border: var(--border-thick);
    border-radius: 24px; box-shadow: var(--shadow-md); padding: 32px 40px;
  }
  .comments-section h3 {
    font-weight: 700; font-size: 1.3rem; margin-bottom: 20px;
    padding-bottom: 14px; border-bottom: var(--border);
    display: flex; align-items: center; gap: 10px;
  }
  @media (max-width: 768px) { .comments-section { padding: 24px 18px; } }

  /* ── SIDEBAR ── */
  .sidebar { display: flex; flex-direction: column; gap: 20px; position: sticky; top: 82px; }
  .sidebar-card {
    background: var(--white); border: var(--border-thick); border-radius: 20px;
    box-shadow: var(--shadow-md); padding: 20px; overflow: hidden;
  }
  .sidebar-card h3 {
    font-weight: 700; font-size: 1rem; margin-bottom: 14px;
    padding-bottom: 10px; border-bottom: var(--border);
    display: flex; align-items: center; gap: 8px;
  }
  .toc-list { list-style: none; display: flex; flex-direction: column; gap: 6px; }
  .toc-list li a {
    text-decoration: none; color: #555; font-size: 0.9rem; font-weight: 600;
    display: flex; align-items: flex-start; gap: 8px; padding: 5px 8px; border-radius: 10px;
    transition: background 0.12s, color 0.12s;
  }
  .toc-list li a:hover { background: var(--green-pale); color: var(--green-dark); }
  .toc-list li a.active { background: var(--green-light); color: var(--green-dark); font-weight: 700; }
  .toc-list li a .toc-num {
    min-width: 20px; height: 20px; border-radius: 50%;
    background: var(--yellow); border: 1.5px solid var(--black);
    display: flex; align-items: center; justify-content: center;
    font-size: 0.72rem; font-weight: 700; flex-shrink: 0; margin-top: 1px;
  }
  .latest-list { list-style: none; display: flex; flex-direction: column; gap: 12px; }
  .latest-list li a {
    text-decoration: none; color: var(--black); display: flex; gap: 12px; align-items: flex-start;
    font-size: 0.88rem; font-weight: 600; line-height: 1.4; transition: color 0.12s;
  }
  .latest-list li a:hover { color: var(--green-dark); }
  .latest-list li a .lp-num {
    min-width: 26px; height: 26px; border-radius: 50%;
    background: var(--green-pale); border: var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 0.75rem; font-weight: 700; flex-shrink: 0;
    box-shadow: 2px 2px 0 var(--black);
  }
  .sidebar-donate {
    background: var(--green); border: var(--border-thick); border-radius: 20px;
    box-shadow: var(--shadow-md); padding: 22px 20px; text-align: center;
  }
  .sidebar-donate .don-emoji { font-size: 2.2rem; margin-bottom: 8px; display: block; }
  .sidebar-donate h3 { font-weight: 700; font-size: 1rem; color: white; margin-bottom: 6px; }
  .sidebar-donate p { font-size: 0.85rem; color: rgba(255,255,255,0.8); margin-bottom: 14px; line-height: 1.5; }
  .sidebar-donate a {
    display: inline-block; background: var(--yellow); color: var(--black);
    border: var(--border); border-radius: 50px; padding: 8px 22px; font-weight: 700;
    font-size: 0.92rem; box-shadow: var(--shadow-sm); text-decoration: none;
    transition: transform 0.12s, box-shadow 0.12s;
  }
  .sidebar-donate a:hover { transform: translate(-1px,-2px); box-shadow: 4px 4px 0 var(--black); }

  /* ── FOOTER ── */
  footer { background: #222; color: rgba(255,255,255,0.65); padding: 28px 50px; text-align: center; position: relative; z-index: 1; }
  footer .brand { font-weight: 700; color: var(--green-light); font-size: 1.3rem; margin-bottom: 6px; }
  footer p { font-size: 0.95rem; font-weight: 500; margin-bottom: 3px; }
  .social-icons { margin-top: 14px; display: flex; gap: 10px; justify-content: center; flex-wrap: wrap; }
  .social-icons a {
    text-decoration: none; color: var(--black); font-weight: 600; font-size: 0.9rem;
    background: var(--green-light); border: var(--border); border-radius: 50px;
    padding: 5px 16px; box-shadow: var(--shadow-sm); transition: transform 0.12s;
  }
  .social-icons a:hover { transform: translate(-1px,-2px); }

  /* Floating donate */
  .donate-tag {
    position: fixed; top: 50%; right: 20px; transform: translateY(-50%);
    background: var(--yellow); border: var(--border-thick); border-radius: 20px;
    padding: 10px 14px; box-shadow: var(--shadow-md); font-weight: 700; font-size: 0.95rem; z-index: 50;
  }
  .donate-tag a { text-decoration: none; color: var(--black); }

  @media (max-width: 1024px) { .donate-tag { display: none; } .sidebar { display: none; } }
  @media (max-width: 768px) { footer { padding: 28px 20px; } }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(16px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .article-hero h1, .article-hero .article-meta { animation: fadeUp 0.5s ease both; }
  .article-body { animation: fadeUp 0.55s 0.1s ease both; }
</style>
